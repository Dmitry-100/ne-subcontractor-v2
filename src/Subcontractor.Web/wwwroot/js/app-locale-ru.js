"use strict";

(function () {
    if (!(window.DevExpress && window.DevExpress.localization && typeof window.DevExpress.localization.locale === "function")) {
        return;
    }

    window.DevExpress.localization.locale("ru");
})();
