# Release Candidate Checklist (Sprint 9)

Date: 2026-04-06

## Build and test baseline

- [x] `dotnet restore` completed.
- [x] `dotnet build Subcontractor.sln --no-restore` is green.
- [x] Unit tests are green.
- [x] Integration tests are green.
- [x] API smoke scenarios updated (`docs/api-smoke.md`).

## Data and migrations

- [x] EF migrations are generated and committed up to current model.
- [ ] Migration dry-run validated on target SQL Server 2016 stand.
- [ ] Backup/restore rehearsal executed on RC environment.

## Security and access

- [x] Seeded role/permission matrix validated by automated test.
- [x] Permission validation report prepared (`docs/permission-validation-report.md`).
- [ ] Role-based access verified on RC environment with real AD/LDAP identities.

## Functional readiness

- [x] Dashboard v1 implemented and documented.
- [x] Unified CSV export endpoints implemented and documented.
- [x] Contract execution planning slice implemented and tested.
- [x] Technical pre-UAT execution report prepared (`docs/uat-execution-report-v1.md`).
- [ ] UAT wave-1 executed using `docs/uat-pack-v1.md`.
- [ ] Defect register updated with UAT results (`docs/defect-register-v1.md`).

## Defect gate

- [ ] No open `Blocker` defects.
- [ ] No open `High` defects without approved workaround.
- [ ] All deferred defects approved by Product owner.

## Operational readiness

- [x] CI workflow present (`.github/workflows/ci.yml`).
- [x] DB migration workflow present (`.github/workflows/db-migrate.yml`).
- [x] Production deployment runbook prepared (`docs/production-deployment-guide.md`).
- [x] Go-live checklist prepared (`docs/go-live-checklist.md`).
- [x] Admin/User operational guides prepared (`docs/admin-guide.md`, `docs/user-quick-start-guide.md`).
- [x] Host topology separation documented (`docs/performance-host-topology-evidence-2026-04-13.md`).
- [x] SQL Core rerun script prepared (`scripts/ci/run-sql-core-tests.sh`).
- [x] Performance measurement contour prepared (`scripts/perf/measure-http-metrics.sh`, `scripts/perf/measure-browser-metrics.mjs`).
- [x] Cache/compression telemetry contour prepared (`scripts/perf/capture-http-cache-compression-telemetry.sh`).
- [x] Performance before/after snapshot documented (`docs/performance-report-v2.md`).
- [x] UI assets fallback contour prepared (primary CDN -> secondary CDN -> local path for DevExpress/SheetJS).
- [x] Performance budget gate script prepared (`npm run perf:budget`).
- [x] Performance regression diff report prepared (`npm run perf:compare`, `artifacts/perf/perf-regression-*.md`).
- [x] Performance evidence completeness check prepared (`scripts/ci/check-performance-evidence-pack.sh`, npm alias `check:perf-evidence`).
- [x] Perf contour paired-manifest strategy enabled (`perf-contour-latest.json` is generated by `perf:contour` and consumed by default `perf:budget` run).
- [x] Baseline pin workflow prepared (`npm run perf:pin-baseline` -> `artifacts/perf/perf-contour-baseline.json`).
- [x] Nightly performance contour gate wired in CI (`performance-budget-nightly`, workflow `ci.yml`).
- [x] Weekly long-run performance contour wired in CI (`performance-budget-long-run-weekly`, artifact `artifacts/perf-long-run` + `perf-trend-latest.md`).
- [x] Dependency governance report wired in CI (`dependency-governance-report`, outdated NuGet artifact).
- [x] SQL staging evidence pack runbook prepared (`docs/sql-performance-evidence-runbook.md` + `scripts/perf/capture-sql-performance-evidence-pack.sh`).
- [x] Local UI assets validator script prepared (`scripts/ci/check-local-ui-assets.sh`).
- [ ] Production-like deployment rehearsal completed.
- [ ] Monitoring/log review completed after RC deployment.

## Sign-off

| Gate | Owner | Status | Date | Notes |
|---|---|---|---|---|
| Engineering sign-off | Tech Lead | Pending |  |  |
| QA sign-off | QA Lead | Pending |  |  |
| Product sign-off | Product Owner | Pending |  |  |
| Operations sign-off | DevOps/Infra | Pending |  |  |
